#include<stdio.h>
#include<conio.h>
main()
{     int n, alpha,digit,character;
      clrscr();
      printf("enter any value:",alpha);
      scanf("%c",&alpha);
      printf("enter any value :",digit);
      scanf("%d",&digit);
      printf("enter any value :",character);
      scanf("%c",&character);
      if(n<=0)
      {

      printf(" %c",alpha);
      }

      else
      {
      printf("%d",digit);
      }
      else
      {
      printf("%c",charcter);
      }

      else
getch();
}